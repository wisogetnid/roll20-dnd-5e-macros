#!/bin/bash

CD="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

echo ----------- copy below ----------------
cat $CD/spells-ability.txt
echo ----------- copy above ----------------
